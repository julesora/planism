services:
  db:
    image: postgres:18
    restart: unless-stopped

    environment:
      POSTGRES_USER: planism
      POSTGRES_PASSWORD: planism
      POSTGRES_DB: planism

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: